name: Build and publish

on:
  workflow_dispatch:
    inputs:
      freescout_version:
        description: "FreeScout version (e.g. 1.8.219)"
        required: true
        type: string
      php_version:
        description: "PHP minor version"
        required: false
        default: "8.4"
        type: string
  push:
    tags:
      - "v*"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      freescout_version: ${{ steps.vars.outputs.freescout_version }}
      php_version: ${{ steps.vars.outputs.php_version }}
      image_tag: ${{ steps.vars.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Resolve build args
        id: vars
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            FS_VER="${{ inputs.freescout_version }}"
            PHP_VER="${{ inputs.php_version }}"
          else
            # Tag form: v<freescout-version> (e.g. v1.8.219)
            FS_VER="${GITHUB_REF_NAME#v}"
            PHP_VER="8.4"
          fi
          echo "freescout_version=$FS_VER" >>"$GITHUB_OUTPUT"
          echo "php_version=$PHP_VER"      >>"$GITHUB_OUTPUT"
          echo "image_tag=${FS_VER}-php${PHP_VER}" >>"$GITHUB_OUTPUT"

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Build image (local load)
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          platforms: linux/amd64
          build-args: |
            FREESCOUT_VERSION=${{ steps.vars.outputs.freescout_version }}
            PHP_VERSION=${{ steps.vars.outputs.php_version }}
          tags: freescout:test
          provenance: false

      - name: Save image tarball
        run: docker save freescout:test -o /tmp/image.tar

      - name: Upload image artifact
        uses: actions/upload-artifact@v4
        with:
          name: image
          path: /tmp/image.tar
          retention-days: 1

  test:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Download image artifact
        uses: actions/download-artifact@v4
        with:
          name: image
          path: /tmp

      - name: Load image
        run: docker load -i /tmp/image.tar

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test deps
        run: pip install -r tests/requirements.txt

      - name: Image + runtime tests
        env:
          IMAGE: freescout:test
        run: pytest -v tests/ --junitxml=report.xml

      - name: Dump freescout container logs on failure
        if: failure()
        run: |
          for c in $(docker ps -a --filter "name=^fs-" --format '{{.Names}}'); do
            echo "--- $c ---"
            docker logs "$c" 2>&1 || true
          done

      - name: Upload pytest report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-report
          path: report.xml
          retention-days: 7

  publish:
    needs: [build, test]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Download image artifact
        uses: actions/download-artifact@v4
        with:
          name: image
          path: /tmp

      - name: Load image
        run: docker load -i /tmp/image.tar

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Push three refs:
      #   <version>-php<minor>   fully qualified (existing)
      #   <version>              plain FreeScout version alias — resolves to whichever
      #                          PHP build this run produced (one PHP variant per run today;
      #                          revisit if multi-PHP fan-out is added)
      #   latest                 existing
      #
      # docker save + docker push preserve the Docker manifest schema (not OCI),
      # which is what the podman HEALTHCHECK rationale in the Dockerfile depends on.
      - name: Tag and push
        env:
          FS_VER: ${{ needs.build.outputs.freescout_version }}
          IMAGE_TAG: ${{ needs.build.outputs.image_tag }}
        run: |
          set -euo pipefail
          REPO="${REGISTRY}/${IMAGE_NAME}"
          docker tag freescout:test "${REPO}:${IMAGE_TAG}"
          docker tag freescout:test "${REPO}:${FS_VER}"
          docker tag freescout:test "${REPO}:latest"
          docker push "${REPO}:${IMAGE_TAG}"
          docker push "${REPO}:${FS_VER}"
          docker push "${REPO}:latest"
